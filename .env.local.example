# Copy to .env.local and fill in. Both vars are also available on the client
# via NEXT_PUBLIC_*. Service role key is server-only — never expose it.
NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
